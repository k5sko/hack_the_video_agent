.env
.env.local
__pycache__/
*.pyc
.venv/
venv/
node_modules/
dist/
.DS_Store
*.mp4
*.mkv
*.webm

# Downloaded source video, chunks, captions and run logs (437MB, re-fetchable
# with fetch_media). data/cache/ is deliberately NOT ignored — the committed
# cache is what lets the demo run with the network cable pulled.
beeline/data/media/
# Cut clips: ~350MB, regenerate with `python -m beeline.ingestion.cut` from the
# sources. data/cache/ stays committed — that is what makes the demo offline.
beeline/data/clips/

*.tsbuildinfo
.pytest_cache/
